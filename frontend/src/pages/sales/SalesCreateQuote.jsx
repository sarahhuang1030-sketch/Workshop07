import React from "react";

export default function SalesCreateQuote() {
    return (
        <div className="container py-4">
            <h2>Create Quote</h2>
            <p>Placeholder: build quote (plan + add-ons + device) and save.</p>
        </div>
    );
}
