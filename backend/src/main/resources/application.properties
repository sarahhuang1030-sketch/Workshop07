# Use the PID using 8081
server.port=8081

# MySQL
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=${DB_DRIVER}
# Hibernate DDL /JPA
#spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.springframework.security=DEBUG  # ??????

#adding sending email's information
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=sarahhuang1030@gmail.com
spring.mail.password=qjbqvxjtrmeibfes
##will add this into environment variable later : Run/Debug Configurations ? Environment variables.
#MAIL_USERNAME=sarahhuang1030@gmail.com
#MAIL_PASSWORD=qjbqvxjtrmeibfes
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

app.frontend.reset-url=${FRONTEND_RESET_URL:http://localhost:5173/resetpassword}

#add this to stop hibernate from converting names to snake_case
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.jpa.hibernate.naming.implicit-strategy=org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl

#adding this because  of token for address
app.jwt.secret=CHANGE_ME_TO_A_LONG_RANDOM_SECRET_AT_LEAST_32_CHARS
app.jwt.expirationMs=86400000

# to store sessions
server.servlet.session.cookie.same-site=lax
server.servlet.session.cookie.secure=false

##locally storing profile picture
app.upload-dir=src/uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
